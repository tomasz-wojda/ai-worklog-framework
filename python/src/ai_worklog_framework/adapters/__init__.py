"""
adapters — Read-only service adapter contracts and implementations.

Each adapter provides a standardized interface for querying external services
without performing mutations. Adapters return Result objects from result.py.
"""
